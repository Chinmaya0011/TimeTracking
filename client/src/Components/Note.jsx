import React from 'react';
import styles from '../assets/styles/Note.module.css';

const Note = () => {
  return (
    <div className={styles.note}>
      For Best View switch turn on pc mode
    </div>
  );
};

export default Note;
